import next from "@next/eslint-plugin-next";

export default [
  {
    files: ["**/*.{js,jsx,ts,tsx}"],
    plugins: {
      next,
    },
    rules: {
      // Add any custom rules here
    },
  },
];
